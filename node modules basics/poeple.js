const philo = ['Plato', 'Aristotle', 'Kant', 'Vivekanand']
const age = [20, 30, 40, 50]

console.log(philo, age);

module.exports = {
    philo,
    age: age //in es6 we can write only once 'age' if key and value is same.
}